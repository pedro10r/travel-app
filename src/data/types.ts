import { ImageSourcePropType } from "react-native";

export type TCard = {
  id: number;
  image: string;
  title: string;
};

export type TPlace = {
  id: number;
  image: string;
  title: string;
  description: string;
};
